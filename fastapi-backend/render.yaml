services:
  - type: web
    name: ai-pipeline-backend
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn main:app --host 0.0.0.0 --port $PORT"
    healthCheckPath: "/chat-api/"
    envVars:
      - key: PYTHON_VERSION
        value: 3.12
